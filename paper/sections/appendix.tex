\section*{Appendix}

\section{Temporal Folding and Geometry}
\label{app:folding}

\subsection{Temporal-folding definition and geometry}
\label{app:folding-definition}

For an input $X\in\mathbb{R}^{C\times T}$ and a divisor $P$ of $T$, the
phase-interleaved adapter produces $F_P(X)\in\mathbb{R}^{CP\times T/P}$
according to
\[
F_P(X)_{cP+p,w}=X_{c,wP+p},\qquad 0\le p<P.
\]
This is a bijective permutation of the $CT$ samples, so folding changes only
the representation geometry. In contrast, a contiguous reshape assigns each
row a consecutive time chunk; both layouts have identical dimensions but
different local neighborhoods. The adapter pads only the bottom and right
edges to satisfy the backbone stride. For stride $s$, the padded dimensions
and non-padding height occupancy are
\[
H_{\rm pad}=s\left\lceil\frac{CP}{s}\right\rceil,\qquad
W_{\rm pad}=s\left\lceil\frac{T/P}{s}\right\rceil,\qquad
\rho_H=\frac{CP}{H_{\rm pad}}.
\]
Increasing $P$ raises vertical occupancy but reduces horizontal temporal
resolution and increases phase-wrap distance. The geometry rule therefore
selects the smallest valid $P$ reaching the target folded height; the complete
fold-factor sweeps are reported in Appendix~\ref{app:p-ablation}.

\paragraph{Exact invertibility.}
The mapping is bijective: for every original sample index
$t=wP+p$, the inverse is
$X_{c,t}=F_P(X)_{cP+(t\bmod P),\lfloor t/P\rfloor}$. Consequently,
$\|F_P(X)\|_0=\|X\|_0$ and no interpolation, averaging, or frequency-domain
operation is introduced. Any difference between temporal folding and contiguous
reshape therefore comes solely from the neighborhoods presented to the
backbone. The examples below visualize this change of coordinates; they are
not claims that the highlighted patterns are uniquely physiological or
causal.

\subsection{Visual interpretation and examples}
\label{app:folding-visuals}

The visualizations provide three complementary checks. First, the waveform
panels establish the original channel--time coordinates. Second, the folding
panels show that phase $p$ becomes a vertical coordinate while $w$ advances
by $P$ samples horizontally. Third, the real and synthetic examples verify
that localized transients, oscillations, and repeated motifs remain present
after the permutation. Pixel values should therefore be compared through the
index relation above, rather than by interpreting the folded image as a new
measurement.

Figure~\ref{fig:folding-examples} presents the examples in a controlled-to-real
progression. The upper panels pair three simulated waveforms with their exact
phase--time renderings; the lower panels show the same correspondence for a
real ictal CHB-MIT segment.

\begin{figure*}[t]
\centering
\includegraphics[width=0.98\textwidth]{combined_folding_examples.pdf}
\caption{Controlled and real examples under lossless $P=4$ temporal folding.
Panels A--F pair three simulated EEG morphologies with the phase--time tensors
formed from the same 1,000 samples. Panels G--I show a labeled ictal CHB-MIT
segment after the downstream preprocessing: all 16 bipolar channels, an
enlarged F7--T7 trace, and the exact $64\times500$ folded tensor. Folding
retains every sample and changes only its two-dimensional neighborhood. The
simulations explain the coordinate geometry and are not diagnostic templates.}
\label{fig:folding-examples}
\end{figure*}
\FloatBarrier

\subsection{What a convolution sees}
\label{app:convolution}
Within one electrode's phase block, a vertical step changes the phase index,
while a horizontal step advances by $P$ original samples. A $k\times k$
convolution therefore mixes offsets
\[
\Delta t=P\Delta w+\Delta p,
\]
where $(\Delta w,\Delta p)$ is a kernel offset in folded coordinates. The
local patch is flattened and multiplied by the kernel weights, followed by
bias and nonlinearity; no special EEG operation is introduced. For $P=4$ and
$k=3$, the direct temporal offsets are
$\{-5,-4,-3,-1,0,1,3,4,5\}$.

At phase-block boundaries a vertical neighborhood can also cross electrodes,
so the interpretation depends on channel ordering. Figure~\ref{fig:channel-crossing}
makes the two regimes explicit. Inside one block, the three columns of a
$3\times3$ window are $P$ samples apart, so the window samples the waveform
like a dilated (atrous) pattern with rate $P$: for $P=4$ its nine taps cover
three consecutive samples around each of $t-P$, $t$, and $t+P$, which is
exactly the offset set above. At a block boundary, however, the $\Delta p=+1$
row is no longer the next phase of the same electrode: row $cP+P-1$ is
adjacent to row $(c+1)P$, so that row reads electrode $c+1$ instead, and its
offsets are measured in the neighbour's own time frame. The crossing therefore
mixes two electrodes only at block boundaries, and the induced neighborhood
depends on channel ordering.

\begin{figure*}[t]
\centering
\includegraphics[width=0.94\textwidth]{channel_crossing.pdf}
\caption{A $3\times3$ window at a phase-block boundary ($P=4$, real TUEV
segment; channels T5--O1 and FP2--F8). (a) Each channel occupies $P$
consecutive phase rows, separated by the bold line. The solid window straddles
the boundary, so its $\Delta p=+1$ row belongs to the next electrode, whereas
the dashed window for comparison stays inside a single electrode. (b) The same
boundary window on the original traces: one row of the window reads $c+1$
(blue), and within each electrode the taps form a dilated pattern with rate
$P=4$, matching $\Delta t=P\Delta w+\Delta p$.}
\label{fig:channel-crossing}
\end{figure*}
\FloatBarrier



\input{sections/appendix_detailed_results}

\section{Matched Foundation-Model Reruns}
\label{app:foundation-rerun}

We also reran CBraMod and REVE on all twelve datasets using the inherited
partitions, seeds 42--46, validation-selected checkpoints, and one final test
evaluation per seed. Table~\ref{tab:foundation-reruns}
lists these reruns next to our finalized vision models, which follow the
identical protocol, so that the two families can be compared under one
pipeline. The downstream pipeline
and optimizer match our local experiments, while each model retains its
released input normalization. These are matched-protocol reruns rather than
exact reproductions of every original training stage; one run is left
blank pending a follow-up tuning pass: CBraMod on TUEV selects the first
epoch for every seed because validation $\kappa$ peaks there. On BCIC2020-3
and SEED-V the released pooled-token readout does not converge on these
arrays, so those reruns use the non-pooling readout (flattened patch tokens
plus the context token) that the released paper employs for its probe columns.

CBraMod's released multi-learning-rate recipe is replaced by one uniform
learning rate. REVE's published linear-probe warm start, mixup, StableAdamW,
LoRA, and model souping are not applied. For TUAB and TUEV, REVE receives the
same 16-channel bipolar arrays used by CBraMod rather than the released
21-channel memmap, at the equivalent microvolt/100 scale; ISRUC keeps the
released loader scale, and BCIC2020-3 and MentalArithmetic use their released
scale factors. REVE on ISRUC additionally runs at batch size 8 rather than the
configured 16 so that the 20-epoch 4D batches fit in memory. CHB-MIT, SEED-V,
and SHU-MI are not part of the released REVE benchmark, so their REVE runs use
fallback specifications (microvolt/100 input scale, pooled-token readout)
and are reported as reference-only. BCIC2020-3 and SEED-V switch to the
non-pooling readout because the released pooled-token readout stays at chance
on these arrays. These deviations can lower
rerun values relative to published results, so the experiment quantifies
protocol sensitivity rather than replacing the original references.

\begin{table*}[t]
\centering\tiny
\caption{Protocol-matched comparison on the twelve rerun datasets. CBraMod and
REVE-Base are rerun under our downstream pipeline
(inherited partitions, seeds 42--46, validation-selected checkpoints, one final
test per seed) and are listed together with our finalized vision models, which
follow the same protocol. Values are five-seed means $\pm$ population standard
deviations; the selection metric is PR-AUC for the binary tasks (TUAB,
CHB-MIT, SHU-MI, Mumtaz2016, MentalArithmetic) and $\kappa$ for the remaining
multiclass tasks. Bold marks the best matched-protocol value in each column;
published values are transcribed from the original benchmark tables and are
not ranked. Dashes mark reruns reserved for a follow-up tuning pass.}
\label{tab:foundation-reruns}
\setlength{\tabcolsep}{5pt}
\setlength{\aboverulesep}{0.25ex}
\setlength{\belowrulesep}{0.4ex}
\renewcommand{\arraystretch}{0.98}
\begin{tabular}{@{}llccc@{}}
\toprule
Dataset & Method & BA & Selection metric & Published \\
\midrule
\multirow{5}{*}{TUAB}
  & CBraMod (rerun) & $0.8066\pm0.0026$ & $0.8923\pm0.0026$ (PR-AUC) & $0.9221$ \\
  & REVE-Base (rerun) & $0.8064\pm0.0053$ & $0.8833\pm0.0062$ (PR-AUC) & $0.9281$ \\
\cmidrule(lr){2-5}
  & EfficientNet-B0 & \bestcell{0.8317\pm0.0033} & \bestcell{0.9178\pm0.0025} (PR-AUC) & -- \\
  & ConvNeXt-Tiny & $0.8229\pm0.0012$ & $0.9076\pm0.0018$ (PR-AUC) & -- \\
  & ViT-Small & $0.8202\pm0.0031$ & $0.9091\pm0.0034$ (PR-AUC) & -- \\
\midrule
\multirow{5}{*}{FACED}
  & CBraMod (rerun) & $0.5531\pm0.0030$ & $0.4939\pm0.0035$ ($\kappa$) & $0.5041$ \\
  & REVE-Base (rerun) & $0.5127\pm0.0249$ & $0.4505\pm0.0274$ ($\kappa$) & $0.5080$ \\
\cmidrule(lr){2-5}
  & EfficientNet-B0 & $0.5516\pm0.0179$ & $0.4953\pm0.0197$ ($\kappa$) & -- \\
  & ConvNeXt-Tiny & \bestcell{0.6660\pm0.0076} & \bestcell{0.6223\pm0.0083} ($\kappa$) & -- \\
  & ViT-Small & $0.4464\pm0.0102$ & $0.3744\pm0.0109$ ($\kappa$) & -- \\
\midrule
\multirow{5}{*}{HMC}
  & CBraMod (rerun) & $0.7287\pm0.0023$ & $0.6752\pm0.0023$ ($\kappa$) & -- \\
  & REVE-Base (rerun) & $0.7343\pm0.0021$ & $0.6813\pm0.0035$ ($\kappa$) & $0.6982$ \\
\cmidrule(lr){2-5}
  & EfficientNet-B0 & \bestcell{0.7557\pm0.0019} & \bestcell{0.7070\pm0.0023} ($\kappa$) & -- \\
  & ConvNeXt-Tiny & $0.7534\pm0.0040$ & $0.7060\pm0.0041$ ($\kappa$) & -- \\
  & ViT-Small & $0.7476\pm0.0032$ & $0.6987\pm0.0033$ ($\kappa$) & -- \\
\midrule
\multirow{5}{*}{Mumtaz2016}
  & CBraMod (rerun) & $0.9008\pm0.0078$ & $0.9767\pm0.0109$ (PR-AUC) & $0.9923$ \\
  & REVE-Base (rerun) & \bestcell{0.9253\pm0.0152} & \bestcell{0.9912\pm0.0056} (PR-AUC) & $0.9961$ \\
\cmidrule(lr){2-5}
  & EfficientNet-B0 & $0.9153\pm0.0061$ & $0.9864\pm0.0045$ (PR-AUC) & -- \\
  & ConvNeXt-Tiny & $0.9080\pm0.0099$ & $0.9789\pm0.0070$ (PR-AUC) & -- \\
  & ViT-Small & $0.9112\pm0.0099$ & $0.9797\pm0.0037$ (PR-AUC) & -- \\
\midrule
\multirow{5}{*}{PhysioNet-MI}
  & CBraMod (rerun) & $0.6183\pm0.0070$ & $0.4910\pm0.0094$ ($\kappa$) & $0.5222$ \\
  & REVE-Base (rerun) & \bestcell{0.6916\pm0.0057} & \bestcell{0.5888\pm0.0076} ($\kappa$) & $0.5306$ \\
\cmidrule(lr){2-5}
  & EfficientNet-B0 & $0.6491\pm0.0125$ & $0.5322\pm0.0167$ ($\kappa$) & -- \\
  & ConvNeXt-Tiny & $0.6558\pm0.0057$ & $0.5411\pm0.0077$ ($\kappa$) & -- \\
  & ViT-Small & $0.6377\pm0.0042$ & $0.5169\pm0.0056$ ($\kappa$) & -- \\
\midrule
\multirow{5}{*}{CHB-MIT}
  & CBraMod (rerun) & $0.7418\pm0.0197$ & $0.3686\pm0.0522$ (PR-AUC) & $0.3689$ \\
  & REVE-Base (rerun) & $0.7316\pm0.0307$ & $0.4623\pm0.0265$ (PR-AUC) & -- \\
\cmidrule(lr){2-5}
  & EfficientNet-B0 & $0.7552\pm0.0599$ & $0.4092\pm0.0536$ (PR-AUC) & -- \\
  & ConvNeXt-Tiny & \bestcell{0.7842\pm0.0658} & \bestcell{0.5095\pm0.0143} (PR-AUC) & -- \\
  & ViT-Small & $0.7204\pm0.0175$ & $0.4759\pm0.0118$ (PR-AUC) & -- \\
\midrule
\multirow{5}{*}{SHU-MI}
  & CBraMod (rerun) & $0.6242\pm0.0050$ & $0.7021\pm0.0103$ (PR-AUC) & $0.7139$ \\
  & REVE-Base (rerun) & $0.6356\pm0.0047$ & \bestcell{0.7187\pm0.0101} (PR-AUC) & -- \\
\cmidrule(lr){2-5}
  & EfficientNet-B0 & $0.6273\pm0.0085$ & $0.6982\pm0.0178$ (PR-AUC) & -- \\
  & ConvNeXt-Tiny & \bestcell{0.6398\pm0.0035} & $0.6963\pm0.0101$ (PR-AUC) & -- \\
  & ViT-Small & $0.6302\pm0.0331$ & $0.6891\pm0.0457$ (PR-AUC) & -- \\
\midrule
\multirow{5}{*}{MentalArithmetic}
  & CBraMod (rerun) & $0.7451\pm0.0232$ & $0.5180\pm0.0370$ (PR-AUC) & $0.6267$ \\
  & REVE-Base (rerun) & $0.6736\pm0.0412$ & $0.6679\pm0.0822$ (PR-AUC) & $0.7470$ \\
\cmidrule(lr){2-5}
  & EfficientNet-B0 & $0.6910\pm0.0418$ & \bestcell{0.7903\pm0.0536} (PR-AUC) & -- \\
  & ConvNeXt-Tiny & \bestcell{0.7583\pm0.0242} & $0.7551\pm0.0251$ (PR-AUC) & -- \\
  & ViT-Small & $0.7507\pm0.0464$ & $0.7249\pm0.0625$ (PR-AUC) & -- \\
\midrule
\multirow{5}{*}{TUEV}
  & CBraMod (rerun) & -- & -- & $0.6744$ \\
  & REVE-Base (rerun) & $0.6587\pm0.0276$ & $0.6724\pm0.0215$ ($\kappa$) & $0.6783$ \\
\cmidrule(lr){2-5}
  & EfficientNet-B0 & $0.6423\pm0.0164$ & $0.6901\pm0.0190$ ($\kappa$) & -- \\
  & ConvNeXt-Tiny & $0.7259\pm0.0155$ & \bestcell{0.7624\pm0.0085} ($\kappa$) & -- \\
  & ViT-Small & \bestcell{0.7261\pm0.0135} & $0.7428\pm0.0127$ ($\kappa$) & -- \\
\midrule
\multirow{5}{*}{ISRUC}
  & CBraMod (rerun) & $0.7671\pm0.0085$ & $0.7390\pm0.0100$ ($\kappa$) & $0.7442$ \\
  & REVE-Base (rerun) & $0.8012\pm0.0037$ & $0.7766\pm0.0046$ ($\kappa$) & $0.7500$ \\
\cmidrule(lr){2-5}
  & EfficientNet-B0 & $0.8025\pm0.0027$ & $0.7705\pm0.0032$ ($\kappa$) & -- \\
  & ConvNeXt-Tiny & $0.8111\pm0.0013$ & $0.7864\pm0.0016$ ($\kappa$) & -- \\
  & ViT-Small & \bestcell{0.8130\pm0.0027} & \bestcell{0.7875\pm0.0026} ($\kappa$) & -- \\
\midrule
\multirow{5}{*}{SEED-V}
  & CBraMod (rerun) & $0.3812\pm0.0033$ & $0.2313\pm0.0045$ ($\kappa$) & $0.2569$ \\
  & REVE-Base (rerun) & $0.3798\pm0.0546$ & $0.2255\pm0.0684$ ($\kappa$) & -- \\
\cmidrule(lr){2-5}
  & EfficientNet-B0 & $0.3660\pm0.0062$ & $0.2056\pm0.0078$ ($\kappa$) & -- \\
  & ConvNeXt-Tiny & $0.3832\pm0.0074$ & $0.2351\pm0.0098$ ($\kappa$) & -- \\
  & ViT-Small & \bestcell{0.3930\pm0.0035} & \bestcell{0.2442\pm0.0045} ($\kappa$) & -- \\
\midrule
\multirow{5}{*}{BCIC2020-3}
  & CBraMod (rerun) & $0.5952\pm0.0103$ & $0.4940\pm0.0129$ ($\kappa$) & $0.4216$ \\
  & REVE-Base (rerun) & $0.5867\pm0.0970$ & $0.4833\pm0.1212$ ($\kappa$) & $0.4543$ \\
\cmidrule(lr){2-5}
  & EfficientNet-B0 & $0.6539\pm0.0128$ & $0.5673\pm0.0160$ ($\kappa$) & -- \\
  & ConvNeXt-Tiny & $0.6387\pm0.0220$ & $0.5483\pm0.0275$ ($\kappa$) & -- \\
  & ViT-Small & \bestcell{0.6619\pm0.0090} & \bestcell{0.5773\pm0.0113} ($\kappa$) & -- \\
\bottomrule
\end{tabular}
\tablenote{Published values are transcribed from the CBraMod and REVE
benchmark tables; HMC is not reported by CBraMod and CHB-MIT, SEED-V, and
SHU-MI are not reported by REVE. Dashes mark runs reserved for a follow-up
tuning pass. The reruns use a common
downstream protocol and are not strict replications of all original training
stages.}
\end{table*}

Under this matched protocol the two foundation models fall below their
published values in 15 of the 19 published comparisons, which quantifies
how much those numbers depend on the original training recipes; the exceptions
are REVE-Base on ISRUC ($\kappa$ $0.7766$ versus $0.7500$) and PhysioNet-MI
($0.5888$ versus $0.5306$), where the rerun also leads every vision backbone in
both columns, and CBraMod and REVE-Base on BCIC2020-3 ($0.4940$ and $0.4833$
versus $0.4216$ and $0.4543$).
Across the twelve datasets the best vision backbone exceeds both reruns on
both balanced accuracy and the selection metric in nine; the three exceptions
are SHU-MI, Mumtaz2016, and PhysioNet-MI, where REVE-Base leads.
EfficientNet-B0 leads all five methods on TUAB and HMC in both columns,
ConvNeXt-Tiny on FACED and CHB-MIT, and ViT-Small on ISRUC, SEED-V, and
BCIC2020-3; TUEV and MentalArithmetic split between backbones across the two
columns. The closest comparisons are ISRUC, where ViT-Small leads REVE-Base by
$0.0109$ $\kappa$, and Mumtaz2016, where REVE-Base edges out the best vision
backbone by $0.0100$
balanced accuracy and $0.0048$ PR-AUC. We therefore read this table as a
protocol-controlled comparison and keep the published values as the external
reference in the main table.

\clearpage
\section{Ablation Studies}
\label{app:ablations}
\input{sections/appendix_c_ablations}

\section{Model-Family Comparison}
\label{app:model-families}

This appendix separates the representational interface from model scale. All
three vision backbones receive the same folded EEG tensor, whereas the EEG
foundation models use their native patching and positional mechanisms. The
comparison is descriptive rather than a runtime benchmark: parameter and FLOP
counts are measured on a shared input and exclude task heads. Using the
released implementations and an explicit PyTorch FLOP counter, the vision
encoders at a shared $32\times1000$ input with $P=4$ require 4.01M/0.247G
(EfficientNet-B0), 27.82M/2.903G (ConvNeXt-Tiny), and 21.39M/3.000G
(ViT-Small) parameters/FLOPs, respectively. FLOPs count one multiply-add as
one operation; attention fast paths are disabled for reproducible accounting.
Table~\ref{tab:model-scale} summarizes the compared model families; folding
itself adds no learned parameters or arithmetic operations, but requires
$O(CT)$ memory movement.

\begin{table*}[tbp]
\centering
\caption{Conceptual scope of the model families discussed in the paper.}
\label{tab:model-scale}
\small
\renewcommand{\arraystretch}{1.06}
\begin{tabularx}{\textwidth}{@{}lXXX@{}}
\toprule
 & \method{} (B0) & \cbramod{} & \reve{}-Base \\
\midrule
Input & phase-folded raw waveform & raw patches + FFT magnitude & raw waveform patches \\
Encoder & EfficientNet-B0 & criss-cross transformer & coordinate-aware transformer \\
EEG pretraining & none & TUH EEG & 92 EEG datasets \\
Encoder parameters & 4.01M & 4.92M & 69.19M \\
Forward compute @$32\times1000$ & 0.25 GFLOPs & 0.79 GFLOPs & 11.07 GFLOPs \\
Spatial identity & row order & learned fixed-order encoding & physical coordinates \\
Main target & full fine-tuning & compact EEG transfer & montage-flexible transfer \\
\bottomrule
\end{tabularx}
\tablenote{The B0 encoder count excludes its unused ImageNet classifier and
the task-specific head. Parameter and compute counts for \cbramod{} and
\reve{}-Base are measured from their released implementations at the shared
$32\times1000$ input; the \cbramod{} paper reports about 4.0M parameters,
whereas its released checkpoint contains 4.92M.}
\end{table*}

\section{Training Recipes and Backbone Input Shapes}
\label{app:recipes}

Table~\ref{tab:recipes} summarizes one representative ConvNeXt-Tiny training
recipe for each dataset. ``Folded'' denotes the non-padded image shape; zero
padding is appended only at the bottom and right. All runs use DINOv3
initialization, AdamW, cosine decay, dropout .1, bfloat16 mixed precision, and
a dataset-specific linear head. Learning rates, epoch budgets, fold factors,
and validation metrics are selected before the final test evaluation.

\begin{table*}[tbp]
\centering
\caption{Finalized phase-interleaved downstream recipes.}
\label{tab:recipes}
\footnotesize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.06}
\begin{tabularx}{\textwidth}{@{}lccrccc@{}}
\toprule
Dataset & $P$ & Folded $H\times W$ & Epochs & LR & WD & Select \\
\midrule
CHB-MIT & 4 & $64\times500$ & 30 & $10^{-4}$ & $5\cdot10^{-4}$ & PR-AUC \\
TUAB & 4 & $64\times500$ & 10 & $10^{-4}$ & $5\cdot10^{-4}$ & PR-AUC \\
TUEV & 4 & $64\times250$ & 5 & $10^{-4}$ & $5\cdot10^{-4}$ & $\kappa$ \\
ISRUC & 12 & $72\times500$ & 15 & $10^{-4}$ & $5\cdot10^{-3}$ & $\kappa$ \\
FACED & 2 & $64\times1000$ & 50 & $10^{-4}$ & $5\cdot10^{-4}$ & $\kappa$ \\
SEED-V & 1 & $62\times200$ & 50 & $10^{-4}$ & $5\cdot10^{-3}$ & $\kappa$ \\
PhysioNet-MI & 1 & $64\times800$ & 30 & $10^{-4}$ & $5\cdot10^{-3}$ & $\kappa$ \\
SHU-MI & 2 & $64\times400$ & 10 & $10^{-4}$ & $5\cdot10^{-4}$ & PR-AUC \\
BCIC2020-3 & 1 & $64\times600$ & 70 & $10^{-5}$ & $5\cdot10^{-3}$ & $\kappa$ \\
Mumtaz2016 & 4 & $76\times250$ & 30 & $10^{-4}$ & $5\cdot10^{-4}$ & PR-AUC \\
MentalArithmetic & 4 & $80\times250$ & 50 & $10^{-4}$ & $5\cdot10^{-4}$ & PR-AUC \\
HMC & 16 & $64\times375$ & 30 & $10^{-4}$ & $5\cdot10^{-3}$ & $\kappa$ \\
\bottomrule
\end{tabularx}
\tablenote{``Select'' is the validation metric used for checkpointing.
ConvNeXt-Tiny uses flatten aggregation except for TUEV, ISRUC, and HMC, which
use GAP.}
\end{table*}

\subsection{timm input-channel adaptation and pretrained checkpoints}
\label{app:timm-input-adaptation}

All vision backbones are instantiated through the timm compatibility layer with
\texttt{timm.create\_model(backbone\_name, pretrained=..., in\_chans=1)}.
Thus the folded EEG tensor is passed as a genuine single-channel image; it is
not copied into three identical channels at the data-loader level. For models
whose released visual checkpoint has a three-channel stem, timm adapts the
first convolution or patch-embedding projection to one input channel when
loading the pretrained weights, using timm's input-channel conversion rule.
All subsequent pretrained parameters are loaded unchanged. Random-
initialization controls use the same \texttt{in\_chans=1} model construction
with \texttt{pretrained=False}, so the input-channel convention is identical
in both conditions.

The EfficientNet-B0 experiments use the ImageNet-1k pretrained timm model
\texttt{efficientnet\_b0}. The ConvNeXt-Tiny experiments use
\texttt{convnext\_tiny.dinov3\_lvd1689m}, and the ViT-Small experiments use
\texttt{vit\_small\_patch16\_dinov3.lvd1689m}. The latter two checkpoints are
the DINOv3 LVD-1689M weights distributed through timm's Hugging Face model
hub integration. We use the public DINOv3 release described by
\citet{oquab2025dinov3}; the checkpoint names above identify the exact timm
variants used by the configurations. No EEG data are used to initialize these
visual weights.

\section{Data and Code Reproducibility Statement}

The codebase records exact dataset paths, input shapes, preprocessing, seeds,
training arguments, logs, and validation-selected checkpoints.  Public data
licenses and access conditions differ by benchmark; no dataset is redistributed
with the paper.  The CHB-MIT manifest follows the subject split of the
\cbramod{} benchmark code (CHB01--20/CHB21--22/CHB23--24 for
train/validation/test) \citep{wang2025cbramod}; the remaining manifests follow
the fixed subject- or trial-level partitions of the \cbramod{} and \reve{}
benchmark code \citep{wang2025cbramod,elouahidi2025reve}, with HMC using the
\reve{} partition \citep{elouahidi2025reve,alvarez2022hmc}.  Reproduction still
requires the documented dataset versions and local split manifests. We use
TUAB v3.0.1 and TUEV v2.0.1; for TUEV, the held-out test partition is the
official evaluation set and contains 29,421 segments in both v2.0.0 and
v2.0.1, while the newer release changes only the training and validation
counts (68,445/15,487 rather than 67,436/15,634).
