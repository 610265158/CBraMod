\section{Experimental Setup}
\label{sec:experiments}

\subsection{Benchmarks and split provenance}

Our suite contains 12 datasets spanning clinical detection (CHB-MIT, TUAB,
TUEV), sleep staging (ISRUC, HMC), emotion recognition (FACED, SEED-V), motor
imagery (PhysioNet-MI, SHU-MI), imagined speech (BCIC2020-3), depression
detection (Mumtaz2016), and mental workload (MentalArithmetic). Table~
\ref{tab:datasets} gives the input shapes and split sizes. All recordings are
represented at 200\,Hz in the common preprocessing pipeline. Regular loaders
use the $[B,C,T]$ convention; ISRUC preserves its sequence of 20 labelled
epochs.

\begin{table*}[t]
\centering
\caption{The 12-dataset evaluation suite and inherited benchmark split sizes.}
\label{tab:datasets}
\small
\renewcommand{\arraystretch}{1.04}
\begin{tabularx}{\textwidth}{@{}lXccrrr@{}}
\toprule
Dataset & Task & Input shape & Classes & Train & Val & Test \\
\midrule
\familyrow{7}{Clinical detection}
CHB-MIT & seizure detection & $16\times2000$ & 2 & 286,964 & 23,065 & 16,964 \\
TUAB & abnormal detection & $16\times2000$ & 2 & 297,103 & 75,407 & 36,945 \\
TUEV & event classification & $16\times1000$ & 6 & 68,445 & 15,487 & 29,421 \\
\familyrow{7}{Sleep staging}
ISRUC & sleep staging & $20\times6\times6000$ & 5 & 3,559 & 468 & 435 \\
HMC & sleep staging & $4\times6000$ & 5 & 91,248 & 22,124 & 23,871 \\
\familyrow{7}{Affective computing}
FACED & emotion recognition & $32\times2000$ & 9 & 6,720 & 1,680 & 1,932 \\
SEED-V & emotion recognition & $62\times200$ & 5 & 34,432 & 42,960 & 40,352 \\
\familyrow{7}{Brain--computer interfaces}
PhysioNet-MI & motor imagery & $64\times800$ & 4 & 6,300 & 1,734 & 1,803 \\
SHU-MI & motor imagery & $32\times800$ & 2 & 7,210 & 2,431 & 2,347 \\
BCIC2020-3 & imagined speech & $64\times600$ & 5 & 4,500 & 750 & 750 \\
\familyrow{7}{Clinical and cognitive state decoding}
Mumtaz2016 & depression & $19\times1000$ & 2 & 4,891 & 1,041 & 1,211 \\
MentalArithmetic & mental workload & $20\times1000$ & 2 & 1,343 & 172 & 192 \\
\bottomrule
\end{tabularx}
\tablenote{Input shapes omit the batch dimension. ISRUC stores 20 consecutive
epochs per record; all other rows are channel $\times$ time. ``Classes''
reports semantic classes, while binary heads use one output logit.}
\end{table*}

We inherit the public split definitions from the CBraMod and REVE benchmark
code and retain the same
train, validation, and test partitions used by the corresponding references
\citep{wang2024cbramod,elouahidi2025reve}. For example, CHB-MIT uses
CHB01--20/CHB21--22/CHB23--24 for train/validation/test. TUEV is prepared from
v2.0.1 rather than v2.0.0, but both releases use the same official 29,421-sample
test set; only the training and validation counts differ.

\subsection{Training and implementation}

All models use AdamW \citep{loshchilov2019decoupled} with cosine decay,
bfloat16 mixed precision, dropout 0.1, and batch size 32 except ISRUC (16) and
MentalArithmetic (64). The ImageNet/DINOv3 backbone is fine-tuned jointly with
a newly initialized linear head, and bias and normalization parameters receive
zero weight decay. Samples are converted to float32, clipped, and scaled
before folding (default $x\mapsto x/32$ after clipping to $[-1024,1024]$), and
the fold factor is fixed before training using Eq.~\ref{eq:p-geometry-rule}.
Dataset-specific learning rates, weight decay, epochs, EMA settings, and
augmentations are recorded in Appendix~\ref{app:recipes}; seeds, checkpoint
selection, and the final-test protocol follow Appendix~\ref{app:protocol}.

\paragraph{Ablation studies.}
We conduct four complementary studies:
\textbf{(1) Cross-backbone consistency.} We repeat the EEG--vision interface
with ImageNet-initialized EfficientNet-B0 and DINOv3-initialized ConvNeXt-Tiny
and ViT-Small, testing robustness across backbone families and visual
pretraining sources. The aggregate comparison is shown in Table~\ref{tab:unified-results};
per-dataset results are provided in Appendix~\ref{app:detailed-results}.
\textbf{(2) Pretraining contribution.} We compare pretrained and randomly
initialized versions of all three visual backbones on TUEV, PhysioNet-MI,
ISRUC, and SHU-MI. Dataset recipes, folding, optimization, checkpoint
selection, and the five-seed test protocol remain fixed within each comparison
(Section~\ref{sec:pretraining-ablation}; Table~\ref{tab:vision-pretraining-ablation}).
\textbf{(3) Folding layout.} We compare phase-interleaved folding with a
contiguous reshape having identical output dimensions, holding all other
settings fixed to isolate temporal layout from height expansion
(Section~\ref{sec:fold-reshape}; Appendix~\ref{app:fold-reshape}).
\textbf{(4) Fold-factor sensitivity.} We sweep $P\in\{1,2,4,8\}$ on CHB-MIT,
TUEV, and MentalArithmetic while keeping the remaining recipe fixed within
each backbone (Appendix~\ref{app:p-ablation}). The geometry rule supplies a
reproducible default; when $P$ is tuned, it is selected using validation data
only.

\subsection{Evaluation protocol}

We compare \method{} with task-specific EEG models and EEG foundation models,
using published baseline results from \cbramod{} and \reve{} under the same
downstream data splits \citep{wang2024cbramod,elouahidi2025reve}. For \cbramod{}
on TUAB and TUEV, we report the variants whose pretraining excludes the target
corpus.

Binary tasks report balanced accuracy (BA), PR-AUC, and ROC-AUC; multiclass
tasks report BA, Cohen's $\kappa$, and weighted F1. Checkpoint selection uses
only validation PR-AUC or validation $\kappa$, while the remaining metrics are
test-time reporting metrics; published BAs are transcribed from the same
benchmark tables and are never mixed across runs. Each recipe is trained with
five seeds (42--46), and we report the arithmetic mean and population standard
deviation after selecting one checkpoint on validation and evaluating it once
on test. The executable protocol is provided in Appendix~\ref{app:protocol}.
